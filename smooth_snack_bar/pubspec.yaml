name: smooth_snack_bar
description: A customizable animated overlay snackbar with swipe dismiss and top/bottom support.
version: 1.0.0
homepage:

environment:
  sdk: ^3.6.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
